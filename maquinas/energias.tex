\documentclass[../principal.tex]{subfiles}
\graphicspath{{\subfix{../imagenes/}}}

\begin{document}

\ifSubfilesClassLoaded{
    \part{Máquinas}
}{}

\chapter{Energías}

La energía es un concepto difuso \footcite{hewitt_fisica_2007}.

\section{Formas de onda}

\subsection{Onda sinusoidal}

Una onda sinusoidal es una onda suave y continua que se repite periódicamente.

La podemos describir con tres parámetros:

\begin{itemize}
    \item Amplitud
    \item Frecuencia
    \item Fase
\end{itemize}

\begin{tikzpicture}
\begin{axis}[
    clip=false,
    xmin=0, xmax=4,
    ymin=-1.5, ymax=1.5,
    axis lines=middle,
    xlabel={$t$ (s)},
    ylabel={$y(t)$},
    xtick={0,1,2,3,4},
    ytick={-1,0,1},
]

% Sine wave: A = 1, T = 2 seconds
\addplot[
    domain=0:4,
    samples=500,
    green,
    thick
]
{sin(deg(2*pi/2 * x))};

% Amplitude lines
\draw[dashed] (axis cs:0,1) -- (axis cs:4,1);
\draw[dashed] (axis cs:0,-1) -- (axis cs:4,-1);

% Amplitude labels
\node[right] at (axis cs:4,1) {$A$};
\node[right] at (axis cs:4,-1) {$-A$};

% Period arrow
\draw[<->] (axis cs:0,-1.3) -- (axis cs:2,-1.3);
\node at (axis cs:1,-1.45) {$T$};

% Optional: function label
\node at (axis cs:3.2,0.8) {$y(t)=A\sin\left(\frac{2\pi}{T}t\right)$};

\end{axis}
\end{tikzpicture}


\subsection{Onda triangular}

La onda triangular tiene una subida y una bajada con la misma pendiente.


\begin{tikzpicture}
\begin{axis}[
    clip=false,
    xmin=0, xmax=4,
    ymin=-1.5, ymax=1.5,
    axis lines=middle,
    xlabel={$t$ (s)},
    ylabel={$y(t)$},
    xtick={0,1,2,3,4},
    ytick={-1,0,1},
]

\addplot[
    domain=0:4,
    samples=500,
    green,
    thick
]
{1 - 4*abs(x/2 - floor(x/2 + 0.5))};

% Amplitude lines
\draw[dashed] (axis cs:0,1) -- (axis cs:4,1);
\draw[dashed] (axis cs:0,-1) -- (axis cs:4,-1);

% Amplitude labels
\node[right] at (axis cs:4,1) {$A$};
\node[right] at (axis cs:4,-1) {$-A$};

% Period arrow
\draw[<->] (axis cs:0,-1.3) -- (axis cs:2,-1.3);
\node at (axis cs:1,-1.45) {$T$};

% Optional: function label
% \node at (axis cs:3.2,0.8) {$y(t)=A\sin\left(\frac{2\pi}{T}t\right)$};

\end{axis}
\end{tikzpicture}

\subsection{Onda diente de sierra}

\subsection{Onda rectangular}

\subsection{Onda cuadrada}

Todas las ondas cuadradas son ondas rectangulares, pero no todas las ondas rectangulares son ondas cuadradas.

Estas ondas son utopías, ya que es imposible generar una onda cuadrada perfecta que cambia de estado de forma instantánea, pero para fines perceptuales podemos aproximarnos a esta forma de onda.

\section{Osciladores controlados por voltaje}

Más conocidos como VCO, por su sigla en inglés, Voltage Controlled Oscillator.

\section{Osciladores de frecuencia baja}

Más conocidos como LFO, por su sigla en inglés, Low Frequency Oscillator.

\newpage

\end{document}

